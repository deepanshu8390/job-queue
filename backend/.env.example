# Backend port. Deployment platforms normally provide this automatically.
PORT=3000

# Exact URL where the frontend is deployed. Do not include a trailing slash.
FRONTEND_ORIGIN=http://localhost:5173

# Optional: use an absolute path or a mounted persistent-volume path in production.
# DATABASE_PATH=C:\path\to\jobs.sqlite
